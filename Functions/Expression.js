//Function Expressions - a function stored in a variable

//Example
function add(x,y) {
    return x+y
}

const sum = function(x,y) {
    return x+y;
}

console.log(add(2,4))
console.log(sum(2,4))

//Both work and give the same result